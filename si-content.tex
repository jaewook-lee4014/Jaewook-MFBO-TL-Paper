%% Supplementary Information content, shared by
%%   si.tex   -> standalone SI file for submission (own title page and reference list;
%%               \ref to main-text figures resolved from main.aux via the xr package), and
%%   main.tex -> combined working PDF when built with \def\withSI{1} (see build.sh).
%% Numbering: Supplementary Notes 1-6, Supplementary Figures 1-3, Supplementary Tables 1-4 (2026-09-14: the earlier Notes 6, 8 and 9, Figs. 3-4 and Tables 5-8 were removed; struck markers show where).
% ---------------------------------------------------------------------------
% Supplementary Information
% Replace the previous \begin{appendices}...\end{appendices} block with this block.
% ---------------------------------------------------------------------------

\ifdefined\withSI\section*{Supplementary Information}\fi
%% Supplementary Notes are unnumbered \section*s, so numbered subsections would print as 0.1, 0.2, ...
\setcounter{secnumdepth}{0}

% Supplementary Note counter for Nature/NCS-style references.
% This lets \ref{suppnote:...} print the Supplementary Note number.
\newcounter{suppnotecounter}
\newcommand{\suppnote}[2]{%
  \refstepcounter{suppnotecounter}%
  \section*{Supplementary Note \thesuppnotecounter. #1}%
  \label{#2}%
}

% Supplementary figure/table numbering.
% These make captions appear as "Supplementary Figure 1" and
% "Supplementary Table 1" in standard LaTeX classes.
\setcounter{figure}{0}
\setcounter{table}{0}
\renewcommand{\figurename}{Supplementary Figure}
\renewcommand{\tablename}{Supplementary Table}

\suppnote{MFGP baseline implementation details}{suppnote:mfgp}

We provide implementation details for the multi-fidelity Gaussian process (MFGP) baseline.

\subsection{Model configuration}

We use \texttt{SingleTaskMultiFidelityGP} from BoTorch~\citep{balandat2020botorch}, which
internally employs a Mat\'{e}rn 5/2 kernel with automatic relevance determination (ARD)
for the input dimensions and a linear truncated kernel for the fidelity dimension.
Supplementary Table~\ref{supptab:mfgp_config} summarizes the key configuration.

\begin{table}[h]
\centering
\caption{MFGP baseline configuration.}
\label{supptab:mfgp_config}
\begin{tabular}{ll}
\toprule
\textbf{Component} & \textbf{Specification} \\
\midrule
GP class & \texttt{SingleTaskMultiFidelityGP} \\
Input kernel & Mat\'{e}rn 5/2 + ARD \\
Fidelity kernel & Linear truncated kernel \\
Outcome transform & \texttt{Standardize(m=1)} \\
Hyperparameter optimization & Exact marginal log-likelihood via L-BFGS-B \\
Fidelity encoding & Last column: 0 = LF, 1 = HF \\
\bottomrule
\end{tabular}
\end{table}

At each iteration the GP was retrained from scratch by maximizing the exact marginal
log-likelihood with L-BFGS-B via \texttt{fit\_gpytorch\_mll}, and predictions were taken
from the posterior at the high-fidelity (HF) level $s=1$. The multi-fidelity GP uses this
single cross-fidelity posterior for acquisition at both fidelity steps.


\suppnote{Uncertainty quantification via Bayesian linear regression}{suppnote:blr}
Uncertainty for the transfer-learning surrogates is obtained from a Bayesian linear regression (BLR) head on the low-fidelity (LF) network's last-layer features, following DNGO~\citep{snoek2015scalable}. Let $\varphi(x) \in \mathbb{R}^D$
denote the penultimate-layer representation and $\tilde{\varphi}(x) = [\varphi(x), 1]$ the
augmented feature vector, with a Gaussian prior over the weights $w$:
\begin{equation}
y \mid x \sim \mathcal{N}\!\left(\tilde{\varphi}(x)^\top w,\, \beta^{-1}\right), \qquad w \sim \mathcal{N}\!\left(0,\, \alpha^{-1} I\right),
\end{equation}
with prior precision $\alpha$ and noise precision $\beta$. The closed-form posterior
yields predictive moments
\begin{equation}
\mu(x) = \tilde{\varphi}(x)^\top m_N, \qquad \sigma^2(x) = \beta^{-1} + \tilde{\varphi}(x)^\top S_N\, \tilde{\varphi}(x),
\end{equation}
where $(m_N, S_N)$ are the posterior mean and covariance\del{ and we set $\alpha = \beta = 1$}. \del{In the primary configuration the LF expected improvement (EI) uses a constant predictive standard deviation, so the BLR head contributes a regularized posterior mean and the acquisition reduces to greedy selection; the five-acquisition uncertainty-driven portfolio that instead uses the BLR predictive standard deviation, and an optional HF BLR head with EI on both fidelities, are evaluated in Supplementary Note~5.}\new{Two such heads are used. The LF head, with $\alpha = \beta = 1$, gives the LF predictive distribution on which the calibration analysis of Fig.~\ref{fig:calibration} is based. The HF head supplies the HF prediction (Methods): it regresses the HF residuals $y^{(H)} - g^{(L)}_{\theta}(x)$ on $[\,g^{(L)}_{\theta}(x),\, \varphi^{(L)}(x),\, 1\,]$, with the ratio of prior to noise precision of the representation block chosen by leave-one-out error over $\{10, 30, 100, 300, 1000\}$, ratios of $10$ and $1$ for the $g^{(L)}_{\theta}$ coefficient and the bias, and the noise precision set to the inverse of the leave-one-out mean squared error, clipped to $[1, 100]$. In the primary configuration both fidelities are queried greedily by the HF predictive mean $\mu^{(H)}$; the five-acquisition portfolio of Supplementary Note~\ref{suppnote:acquisition_variants} instead selects the LF query from the HF predictive mean and standard deviation of this head, using the HF incumbent.}


\suppnote{Implementation details}{suppnote:implementation}

All transfer-learning surrogates share a common architectural backbone, with model-specific training
configurations detailed below.

\subsection{Common settings}
\label{suppsec:common_settings}
Supplementary Table~\ref{supptab:common_hyperparams} summarizes the shared hyperparameters across all deep
surrogates.

\begin{table}[h]
\centering
\caption{Shared hyperparameters across all transfer-learning surrogates.}
\label{supptab:common_hyperparams}
\begin{tabular}{lp{8.5cm}}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Hidden dimension & 64 \\
Number of layers & 2 (for each of LF and HF networks) \\
Activation & Tanh \\
Optimizer & Adam \\
$\ell_2$ weight decay & \del{1e-4}\new{1e-4 for Sequential, Stop-Gradient Joint and End-to-End Joint; none otherwise} \\
\del{BLR prior precision $\alpha$}\new{LF BLR head (calibration)} & \del{1.0}\new{$\alpha = \beta = 1$} \\
\del{BLR noise precision $\beta$}\new{HF BLR head (prediction)} & \del{1.0}\new{prior-to-noise precision ratio of the representation block chosen by leave-one-out error over $\{10, 30, 100, 300, 1000\}$; $\beta = 1/\mathrm{MSE}_{\mathrm{LOO}}$, clipped to $[1, 100]$} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Model-specific settings}
\label{suppsec:model_settings}
Supplementary Table~\ref{supptab:model_hyperparams} presents the training hyperparameters for each transfer-learning
method. \del{Sequential methods use 200 LF epochs followed by 100 HF epochs, while
joint training methods use 300 epochs for simultaneous LF and HF optimization.}\new{Most sequential methods use 200 LF epochs followed by 100 HF epochs (Progressive and Pretrain-then-Joint pretrain for 100), and the joint methods train for 300 epochs (Soft Parameter Sharing for 200).}

\begin{table}[h]
\centering
\caption{Training hyperparameters for each transfer-learning method. LR denotes learning rate.\new{ Weight decay ($\ell_2$, 1e-4) is used only where stated; the HF-network settings of the three configurations reported as feature-extraction transfer are given in the footnote.}}
\label{supptab:model_hyperparams}
{\footnotesize%
\begin{tabular}{@{}lcccc>{\raggedright\arraybackslash}p{5.0cm}@{}}
\toprule
\textbf{Model} & \textbf{LF LR} & \textbf{HF LR} & \textbf{LF Epochs} & \textbf{HF Epochs} & \textbf{Method-Specific Parameters} \\
\midrule
Sequential & 1e-3 & 1e-3 & 200 & 100 & LF frozen during HF training\new{; weight decay 1e-4} \\
Progressive & 1e-3 & 1e-3 $\rightarrow$ 1e-4 & \del{200}\new{100} & 50+50 & Stage 1: head only; Stage 2: full finetune \\
\del{Curriculum}\new{Feature-extraction transfer$^{*}$} & 1e-3 & \del{1e-3}\new{--} & 200 & \del{100}\new{--} & \del{HF samples added gradually}\new{LF network frozen; HF prediction from the BLR head} \\
Pretrain-then-Joint & 1e-3 & 1e-4 & \del{200}\new{100} & 100 & Stage 1: LF only; Stage 2: joint training \\
Stop-Gradient Joint & 1e-3 & 1e-3 & 300 & (joint) & $\alpha_{\text{loss}}$=0.5; stop-gradient on LF$\rightarrow$HF\new{; weight decay 1e-4} \\
End-to-End Joint & 1e-3 & \del{1e-3}\new{5e-4} & 300 & (joint) & Full gradient flow\new{; weight decay 1e-4} \\
Soft Parameter Sharing & 1e-3 & 1e-3 & \del{300}\new{200} & (joint) & $\lambda_{\text{soft}}$=0.01 \\
\ifredline\del{Knowledge Distillation} & \del{1e-3} & \del{1e-3} & \del{200} & \del{100} & \del{$\alpha_{\text{KD}}$=0.3; $T$=3.0} \\ \fi
Domain Adaptation (MMD) & 1e-3 & \del{1e-3}\new{1e-4} & 200 & 100 & $\lambda_{\text{MMD}}$=0.1\new{; both networks updated in the HF stage} \\
\ifredline\del{Pseudo-Labelling} & \del{1e-3} & \del{1e-3} & \del{200} & \del{100} & \del{$w_{\text{pseudo}}$=0.5} \\ \fi
Adapter & 1e-3 & 1e-3 & 200 & 100 & $d_{\text{bottleneck}}$=16; backbone frozen \\
\bottomrule
\end{tabular}%
\par\smallskip
\new{$^{*}$Run as the Curriculum, Knowledge Distillation and Pseudo-Labelling configurations, whose HF (residual) networks are trained for 200 epochs at lr 1e-3 with HF samples added gradually, for 100 epochs at lr 1e-4 with $\alpha_{\text{KD}}$=0.3 and $T$=3.0, and for 100 epochs at lr 1e-4 with $w_{\text{pseudo}}$=0.5, respectively; the residual network does not enter the prediction, so the three give identical results.}%
}
\end{table}

\subsection{Training procedures and method-specific parameters}
\label{suppsec:training_procedures}
\del{All models use $\ell_2$ weight decay (WD=1e-4).}\new{Weight decay ($\ell_2$, 1e-4) is applied by Sequential, Stop-Gradient Joint and End-to-End Joint; the other configurations use none.} \emph{Sequential}: the LF network is
trained first and frozen, then the HF residual network is trained on top.
\emph{Progressive}: Stage 1 trains only the HF head with the backbone frozen; Stage 2
finetunes the full network at a reduced learning rate. \del{\emph{Curriculum}: at epoch $e$ the
number of HF samples used is $n_{\text{use}} = \min(n, \max(2, e/200 \cdot n))$.}\new{\emph{Feature-extraction transfer}: the LF network is pretrained for 200 epochs and frozen, and the HF prediction is the BLR correction of Supplementary Note~\ref{suppnote:blr}; it was run as the Curriculum (HF samples added gradually, $n_{\text{use}} = \min(n, \max(2, e/200 \cdot n))$ at epoch $e$), Knowledge Distillation ($\alpha_{\text{KD}}=0.3$, $T=3.0$) and Pseudo-Labelling ($w_{\text{pseudo}}=0.5$) configurations, which differ only in the training of the residual network and give identical predictions.}
\emph{Pretrain-then-Joint}: Stage 1 pretrains on LF data; Stage 2 jointly trains on both
fidelities at a reduced learning rate. \emph{Stop-Gradient Joint}: joint training with
stop-gradient on the LF network and loss weighting $\mathcal{L} = \alpha_{\text{loss}}
\mathcal{L}_{\text{LF}} + (1-\alpha_{\text{loss}})\mathcal{L}_{\text{HF}}$,
$\alpha_{\text{loss}}=0.5$. \emph{End-to-End Joint}: joint training with full gradient flow\new{ (learning rates 1e-3 for the LF and 5e-4 for the HF network)}. \emph{Soft Parameter Sharing}: penalty
$\lambda_{\text{soft}}\|\theta^{(L)}-\theta^{(H)}\|_2^2$, $\lambda_{\text{soft}}=0.01$.
\del{\emph{Knowledge Distillation}: HF student trained with task loss plus distillation loss,
$\alpha_{\text{KD}}=0.3$, $T=3.0$.} \emph{Domain Adaptation (MMD)}: maximum-mean-discrepancy penalty $\lambda_{\text{MMD}}=0.1$ aligning LF and HF features\new{, with both networks updated at learning rate 1e-4 in the HF stage}. \del{\emph{Pseudo-Labelling}: LF
pseudo-labels weighted $w_{\text{pseudo}}=0.5$.} \emph{Adapter}: frozen LF backbone with a
bottleneck adapter ($d_{\text{bottleneck}}=16$) and HF head.

\suppnote{Synthetic function definitions}{suppnote:synthetic_functions}

Both synthetic functions are standard multi-fidelity Bayesian optimization (MFBO) benchmarks adopted from~Sabanza-Gil et
al.~\citep{sabanza2025best}.

\subsection{Branin function}
The HF Branin function is
\begin{equation}
f^{(H)}(x_1, x_2) = \left(x_2 - b x_1^2 + c x_1 - 6\right)^2 + 10\left(1 - \frac{1}{8\pi}\right)\cos(x_1) + 10,
\end{equation}
with $b = 5.1/(4\pi^2)$, $c = 5/\pi$, domain $x_1 \in [-5, 10]$, $x_2 \in [0, 15]$, and
global minimum $f^* = 0.397887$. The LF approximation modifies only the
quadratic coefficient, $b_{\text{LF}} = b - 0.1(1-\alpha)$, so that the correlation
degrades as $\alpha$ decreases.

\subsection{Park function}
The HF Park function is
\begin{equation}
f^{(H)}(x_1, x_2, x_3, x_4) = \frac{x_1}{2}\sqrt{1 + \frac{(x_2 + x_3^2)x_4}{x_1^2}} + (x_1 + 3x_4)\exp\!\left(1 + \sin(x_3)\right),
\end{equation}
with $x_i \in [0,1]$ and global minimum $f^* = 0$. The LF approximation modifies
the $x_4$ coefficient ($3 \rightarrow 3 - 1.5(1-\alpha)$) and scales the exponential
argument by $\alpha$.

\subsection{Scenario configurations}
Supplementary Table~\ref{supptab:synthetic_scenarios} lists the $\alpha$ values and resulting
characteristics. The $R^2$ values were computed by sampling 1,000 uniform points,
evaluating both fidelities, and fitting a linear regression from LF to HF. Candidate pools
were constructed by uniform grid discretization ($50^2 = 2{,}500$ for Branin; $4^4 = 256$
for Park).

\begin{table}[h]
\centering
\caption{Synthetic function scenario configurations.}
\label{supptab:synthetic_scenarios}
\begin{tabular}{llccccc}
\toprule
\textbf{Benchmark} & \textbf{Scenario} & $\alpha$ & $R^2$ & $\rho$ & \textbf{Pool Size} & $f^*$ \\
\midrule
Branin-Fav & Favourable & 0.8 & 0.97 & 0.1 & 2,500 & 0.398 \\
Branin-Unfav & Unfavourable & 0.1 & 0.56 & 0.5 & 2,500 & 0.398 \\
Park-Fav & Favourable & 0.6 & 0.97 & 0.1 & 256 & 0.0 \\
Park-Unfav & Unfavourable & 0.0 & 0.72 & 0.5 & 256 & 0.0 \\
\bottomrule
\end{tabular}
\end{table}


\suppnote{Acquisition variants and calibration}{suppnote:acquisition_variants}

\del{This section specifies the protocol for each ablation summarized in the main text. The activation ablation is tabulated in Supplementary Table~5; the acquisition-portfolio results are shown in Fig.~5}\new{This section specifies the protocol of the matched-acquisition and acquisition-portfolio comparisons summarized in the main text; their results are shown in Supplementary Figs.~\ref{suppfig:acquisition} and~\ref{suppfig:portfolio}} and their aggregated result files are included in the code release\del{; per-benchmark results of the remaining ablations are available from the corresponding author on request}.

\paragraph{Acquisition variants.} Beyond the primary greedy policy, we evaluated a
portfolio of five uncertainty-driven acquisitions spanning the standard taxonomy of myopic
acquisition functions~\citep{shahriari2015taking}: the improvement-based EI~\citep{jones1998efficient} and probability of
improvement~\citep{kushner1964new}; the optimistic Gaussian-process lower confidence
bound~\citep{srinivas2012information} ($\beta=2$); the information-theoretic max-value
entropy search~\citep{wang2017max} (Gumbel approximation, ten max-value samples); and
sampling-based Thompson sampling~\citep{russo2018tutorial}. Each replaces only the LF acquisition\del{, using the BLR predictive standard deviation}\new{, evaluated on the HF predictive mean and BLR standard deviation of the HF head (Supplementary Note~\ref{suppnote:blr}) with the HF incumbent,} with the surrogate, round-robin schedule, HF step, and seeds held fixed, and was run across all
\del{eleven}\new{nine} transfer-learning surrogates and nine benchmarks (\del{$11\times 9\times 20$}\new{$9\times 9\times 20$} seeds per
acquisition\new{, seeds 42--61}). For each acquisition and benchmark we report the fraction of surrogates on
which it beats the greedy policy\new{ and the mean paired difference in attainment}\del{: the greedy policy beat all five acquisitions on COFs on ten of the
eleven surrogates, whereas Thompson sampling beat the greedy policy on every surrogate on FreeSolv and
polarizability, and the improvement-based acquisitions ranked weakest overall}\new{: no acquisition raises the mean attainment by more than $0.05$ on any benchmark (probability of improvement on Branin-Fav, Thompson sampling on Branin-Unfav), Thompson sampling lowers it by $0.11$ on HOPV15 and $0.13$ on Matbench-gap, and the lower confidence bound and max-value entropy search lower it by $0.06$--$0.10$ on the Branin functions}
(\del{Fig.~5}\new{Supplementary Fig.~\ref{suppfig:portfolio}}). \del{We additionally evaluated a fully uncertainty-aware variant
that adds a second BLR head on the HF network and applies EI at both fidelities.}
As a matched control for the surrogate-versus-acquisition question, the baseline MFGP was also run
with a greedy policy that selects by its posterior mean, all other settings held fixed
(\del{Fig.~4}\new{Supplementary Fig.~\ref{suppfig:acquisition}}).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{paper_figures/supp_acq_matrix.pdf}
\caption{\new{Surrogate $\times$ acquisition comparison under attainment. \textbf{a--i,} Attainment (the Fig.~\ref{fig:final_regret} rule: the mean over the pool's top 5, 2, 1 and 0.5\% targets of the share of the budget window spent at or below the target) for the baseline MFGP with its default expected-improvement (EI) acquisition and with the greedy policy, and for the End-to-End Joint Training surrogate with its default greedy policy and with EI driven by its BLR predictive standard deviation; bars, mean $\pm$ s.e.\ over 40 seeds (42--81; FreeSolv MFGP with EI, $n = 39$, one failed fit). Budgets as in Fig.~\ref{fig:final_regret} except Matbench-gap, evaluated at a budget of 20. \textbf{j,} Attainment with EI minus attainment with the greedy policy, paired per seed and averaged, for the baseline MFGP (40 seeds) and each of the nine transfer-learning surrogates (seeds 42--61); positive values mean that EI attains more.}}
\label{suppfig:acquisition}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{paper_figures/supp_acq_portfolio.pdf}
\caption{\new{Acquisition portfolio under attainment. \textbf{a,} Share of the nine transfer-learning surrogates for which the uncertainty-driven acquisition (EI, probability of improvement, the GP lower confidence bound, max-value entropy search or Thompson sampling, each using the BLR predictive standard deviation) attains more than the greedy policy on each benchmark. \textbf{b,} Mean paired difference in attainment (acquisition minus greedy) over the nine surrogates. \textbf{c--k,} Attainment per acquisition, mean $\pm$ s.e.\ over the nine surrogates of each surrogate's mean over 20 seeds (42--61); budgets as in Fig.~\ref{fig:final_regret} except Matbench-gap at a budget of 20.}}
\label{suppfig:portfolio}
\end{figure*}

\del{[The activation comparison ($\tanh$ versus ReLU; former Supplementary Table~5) is removed.]}

\paragraph{Calibration.} For every surrogate we computed the LF expected
calibration error, negative log-likelihood, and sharpness on held-out candidates and
related them to \del{the final optimization regret}\new{the attainment of Fig.~\ref{fig:final_regret}}; the resulting correlation is reported in
the main text (Fig.~\ref{fig:calibration}\new{a--m}).

\del{\textbf{Uncertainty placement, candidate masking, and adaptive scheduling.} Three other ablations were run: the placement of the BLR head (LF only, HF only, or both); the candidate-masking rule, excluding LF-evaluated candidates from HF evaluation instead of the default rule under which each fidelity excludes only the candidates it has already evaluated; and replacing the round-robin schedule with an adaptive cost-weighted policy. Each was evaluated on a representative subset of three to five benchmarks spanning the favourable and unfavourable regimes, and on every benchmark tested none altered the qualitative ranking of the surrogate families.}




\del{[The former Supplementary Note~6 (family-split advantage maps: complete grid and robustness), Supplementary Figs.~3 and~4 and Supplementary Table~6 are removed; the final-regret maps are Fig.~\ref{fig:family_heatmap} of the main text.]}

\suppnote{Computational cost: absolute FLOPs and compute ranking}{suppnote:computational_cost}

Fig.~\ref{fig:scaling_law} establishes how surrogate compute scales with problem size;
here we report the absolute cost that underlies those laws. The total floating-point operations (FLOPs) of the
MFBO loop (one surrogate fit plus one pool-wide prediction per iteration,
summed over the loop) are shown per surrogate and benchmark, \del{confirming that the
$\mathcal{O}(N^3)$ Gaussian processes dominate the budget while the $\mathcal{O}(N)$
transfer-learning surrogates stay one to two orders of magnitude cheaper}\new{profiled for the surrogates of Figs.~\ref{fig:final_regret} and~\ref{fig:regret_trajectory} (BLR head included): the sparse variational and deep-kernel GPs cost up to $43\times$ and $28\times$ the cheapest transfer-learning surrogate and the baseline MFGP up to $11\times$, whereas on the two smallest pools and budgets (Park-Unfav and HOPV15) the baseline MFGP is the cheapest surrogate of all. FLOPs are accumulated over the budgets of Fig.~\ref{fig:final_regret}, except that FreeSolv is profiled over a budget of 50 and Matbench-gap over 20}.

\begin{figure*}[t]
\centering
%%\includegraphics[width=\textwidth]{paper_figures/computing_time.pdf}
\includegraphics[width=\textwidth]{paper_figures/computing_time_blr.pdf}
\caption{Absolute computational cost across nine \new{of the thirteen} benchmarks, in floating-point operations (FLOPs), a hardware-independent measure: total FLOPs of the MFBO
loop (one surrogate fit plus one pool-wide prediction per iteration, summed over the loop),
with the Gaussian-process family in salmon and the transfer-learning surrogates in blue.
The sparse variational (\del{Sparse MFGP}\new{SV-MFGP}) and deep-kernel (DKL) GPs are the most expensive on
every benchmark, up to roughly \del{$45\times$ and $30\times$}\new{$43\times$ and $28\times$} the fastest transfer-learning surrogate; the
baseline MFGP \del{and NARGP }scale\new{s} as $\mathcal{O}(N^3)$ in the number of observations (measured
fit-FLOP exponent $k = 3.0$) against $\mathcal{O}(N)$ for the transfer-learning surrogates (\del{$k = 1.0$}\new{$k = 0.9$--$1.0$}),
so the gap widens with the evaluation budget. Panel \textbf{j} summarizes these costs as
the average compute rank across all nine benchmarks (lower is cheaper), with the colour
split repeated in a shared legend below the panels.\new{ FLOPs are profiled for the surrogates of the main text (BLR head and the unused residual network included) and the three-member GP family; the transfer-learning configurations are listed individually, so Curriculum (Curr), Knowledge Distillation (KD) and Pseudo-Labelling (PL), reported as feature-extraction transfer in the main text, appear separately; the other abbreviations are those of Fig.~\ref{fig:final_regret}. FLOPs are accumulated over the budgets of Fig.~\ref{fig:final_regret}, except that FreeSolv is profiled over a budget of 50 and Matbench-gap over 20. On Park-Unfav and HOPV15 the baseline MFGP is the cheapest surrogate of all.}}
\label{suppfig:computing_time}
\end{figure*}


\del{[The former Supplementary Notes~8 and~9 (omnibus rank analysis of the average-rank panel; effect sizes for the paired surrogate comparisons) and Supplementary Tables~7 and~8 are removed: the revised main text reports means without significance tests.]}
